var mongoose = require('mongoose');
var Schema = mongoose.Schema;

var Page = new Schema({
  number: {
    type: Number
  },
  nextnumber: {
    type: Number
  },
  nextnumber: {
    type: String
  },
});
